{
  "name": "subscription-management-root",
  "version": "1.0.0",
  "description": "Subscription Management Web Application - Full Stack Root",
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,magenta\" \"npm --prefix backend run start\" \"npm --prefix frontend run dev\"",
    "start": "concurrently -n \"backend,frontend\" -c \"blue,magenta\" \"npm --prefix backend run start\" \"npm --prefix frontend run dev\"",
    "backend": "npm --prefix backend run start",
    "backend:dev": "npm --prefix backend run dev",
    "frontend": "npm --prefix frontend run dev",
    "db:init": "npm --prefix backend run db:init",
    "db:seed": "npm --prefix backend run db:seed",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build"
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
